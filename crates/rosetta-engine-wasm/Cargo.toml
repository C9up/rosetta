[package]
name = "rosetta-engine-wasm"
version = "0.1.5"
edition = "2021"
description = "WASM bindings for the Rosetta i18n engine"

[lib]
crate-type = ["cdylib"]

[dependencies]
rosetta-engine = { path = "../rosetta-engine" }
wasm-bindgen = "0.2"
serde_json = "1"
